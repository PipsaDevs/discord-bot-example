/**
 * This is where we specify the types of Logs
 * It is designed so that the higher the number the more important the log is
 */
enum LogType {
	DEBUG = 0,
	WARNING = 1,
	ERROR = 2,
}

export default LogType;
